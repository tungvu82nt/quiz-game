[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Tắt auto-install extensions để tránh lỗi neon extension
[build.processing]
  skip_processing = false

# Redirects cho SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


